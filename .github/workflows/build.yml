name: Build release packages

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu 22.04 x86_64
            os: ubuntu-22.04
            artifact: YvLink-ubuntu-22.04-x86_64
            archive: YvLink-ubuntu-22.04-x86_64.tar.gz
          - name: Ubuntu 24.04 x86_64
            os: ubuntu-24.04
            artifact: YvLink-ubuntu-24.04-x86_64
            archive: YvLink-ubuntu-24.04-x86_64.tar.gz
          - name: Windows Server 2022 x86_64
            os: windows-2022
            artifact: YvLink-windows-2022-x86_64
            archive: YvLink-windows-2022-x86_64.zip

    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Rust 1.85
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.85.0 --profile minimal
          rustup component add rustfmt --toolchain 1.85.0
          rustup override set 1.85.0
          rustc --version
          cargo --version

      - name: Check formatting
        if: matrix.os == 'ubuntu-24.04'
        run: cargo fmt --check

      - name: Run tests
        run: cargo test --locked --all-targets

      - name: Build release binary
        run: cargo build --locked --release

      - name: Package Linux release
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p package
          cp target/release/mc-proxy package/
          cp README.md config.example.toml package/
          cp MODDED_COMPATIBILITY.md CROSSPLAY.md package/
          tar -czf "${{ matrix.archive }}" -C package .

      - name: Package Windows release
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path package
          Copy-Item target/release/mc-proxy.exe package/
          Copy-Item README.md, config.example.toml, MODDED_COMPATIBILITY.md, CROSSPLAY.md package/
          Compress-Archive -Path package/* -DestinationPath "${{ matrix.archive }}"

      - name: Upload release package
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.archive }}
          if-no-files-found: error
          retention-days: 14
